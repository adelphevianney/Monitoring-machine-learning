# =============================================================================
# requirements/app.txt
# Dépendances du code applicatif Python seul (src/).
#
# Utilisé pour :
#   - Tests unitaires en CI (sans Airflow ni MLflow)
#   - Scripts CLI locaux (generator.py, drift_metrics.py --help)
#   - Linting / type checking (mypy, ruff)
#
# C'est le sous-ensemble minimal pour faire tourner src/ sans infrastructure.
# =============================================================================

# ── Stockage objet ────────────────────────────────────────────────────────────
boto3>=1.34.0

# ── Base de données ───────────────────────────────────────────────────────────
psycopg2-binary>=2.9.5

# ── ML / Data ─────────────────────────────────────────────────────────────────
scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=15.0.0
joblib>=1.3.0

# ── Stats / Drift metrics v2 ──────────────────────────────────────────────────
scipy>=1.11.0

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0