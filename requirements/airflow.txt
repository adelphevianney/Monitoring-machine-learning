# =============================================================================
# requirements/airflow.txt
# Dépendances pour les conteneurs Airflow (webserver + scheduler + init).
# Installé dans docker/airflow/Dockerfile — étend apache/airflow:2.8.1-python3.11
# =============================================================================

# ── Airflow providers ─────────────────────────────────────────────────────────
apache-airflow-providers-postgres==5.10.0
apache-airflow-providers-amazon==8.19.0

# ── ML / Data ─────────────────────────────────────────────────────────────────
scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=15.0.0
joblib>=1.3.0

# ── MLOps ─────────────────────────────────────────────────────────────────────
# Client uniquement — le serveur MLflow tourne dans son propre conteneur
mlflow==2.10.0
cloudpickle>=2.2.0

# ── Stockage objet ────────────────────────────────────────────────────────────
# boto3 uniquement — SDK minio natif supprimé (migration v1 → v2)
boto3>=1.34.0

# ── Base de données ───────────────────────────────────────────────────────────
# psycopg2-binary : précompilé, évite de compiler depuis les sources
# libpq-dev et gcc ne sont plus nécessaires dans le Dockerfile
psycopg2-binary>=2.9.5

# ── Stats / Drift metrics v2 ──────────────────────────────────────────────────
scipy>=1.11.0              # KS test, Chi², Wasserstein distance

# ── Elasticsearch ─────────────────────────────────────────────────────────────
# es_client.py utilise urllib stdlib par défaut.
# Ce package est conservé pour les requêtes avancées éventuelles.
# Doit matcher la version ES dans docker-compose (8.12.0).
elasticsearch==8.12.0

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0